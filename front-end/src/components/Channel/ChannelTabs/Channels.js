import React from "react";
import { Box, styled } from "@mui/material";
import { NoContent } from "./Styling";

export const Channels = () => {
  return (
    <div>
      <NoContent>This channel doesn't feature any other channels.</NoContent>
    </div>
  );
};
